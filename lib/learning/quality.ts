import type {LearningQuestion,Flashcard} from "./types";export function auditQuestion(q:LearningQuestion){const e:string[]=[];if(q.status==="verified"&&!q.sourceIds.length)e.push("Verified question requires source IDs");if(q.options.length<2)e.push("Question needs at least two options");if(q.answerIndex<0||q.answerIndex>=q.options.length)e.push("Answer index is invalid");if(!q.explanation.trim())e.push("Explanation is required");return e}export function auditFlashcard(f:Flashcard){const e:string[]=[];if(f.status==="verified"&&!f.sourceIds.length)e.push("Verified flashcard requires source IDs");if(!f.front.trim()||!f.back.trim())e.push("Front and back are required");return e}