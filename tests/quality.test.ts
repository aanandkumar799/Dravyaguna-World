import {describe,it,expect} from "vitest";import {canPublishPlant} from "../lib/plant/publication";import {auditQuestion} from "../lib/learning/quality";describe("publication gates",()=>{it("blocks draft plants",()=>expect(canPublishPlant({status:"draft"} as any).ok).toBe(false));it("blocks unsupported verified questions",()=>expect(auditQuestion({status:"verified",options:["A"],answerIndex:4,explanation:"",sourceIds:[]} as any).length).toBeGreaterThan(0));});